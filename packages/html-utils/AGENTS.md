# @laughtrack/html-utils

Shared HTML parsing and text extraction utilities.

## Agent Guidelines
- Pure TypeScript, no DOM dependencies.
- Can be used in React Native, Web, and Node environments.
- Use `extractTitleFromHtml` for short titles.
- Use `extractTextFromHtml` for search indexing.
